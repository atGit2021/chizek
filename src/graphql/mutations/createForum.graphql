# import ForumFragment from '../fragments/forum.fragment.graphql'

mutation CreateForum($createForumInput: CreateForumInput!) {
  createForum(createForumInput: $createForumInput) {
    ...ForumFragment
  }
}
